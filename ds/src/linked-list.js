import Node from './node.js'

export default class LinkedList {
    constructor() {

    }

    print() {
        console.log('from linked list')
    }
    /*
        ~~~~~ METHODS ~~~~~
        length(): returns number of nodes in list
        isEmpty(): returns true if length of list is 0
        clear(): removes all elements from this list
        remove(i): removes node at index i
        get(i): return node at index i 
        set(i, data): updates data of node at index i
        add(i, newNode): inserts node at i'th position in list
        addFirst(node): inserts node at front of list
        getFirst(node): gets first node
        indexOf(data): returns index of first occurence of data

    */
}

//testing
let ll = new LinkedList()
